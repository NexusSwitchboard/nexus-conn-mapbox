{
  "name": "@nexus-switchboard/nexus-conn-mapbox",
  "version": "0.0.1",
  "keywords": [
    "nexus-connection"
  ],
  "description": "Client connection to the public github server",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@mapbox/mapbox-sdk": "^0.11.0",
    "@nexus-switchboard/nexus-core": "^0.6.2",
    "assert": "^2.0.0",
    "debug": "^4.1.1"
  },
  "devDependencies": {
    "@types/mapbox__mapbox-sdk": "^0.6.2",
    "@types/debug": "^4.1.5",
    "@types/node": "^12.12.29",
    "tslint": "^6.1.2",
    "typescript": "^3.9.7"
  }
}
